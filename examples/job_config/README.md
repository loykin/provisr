# Job Configuration Example

This example shows how to configure jobs using TOML files with various job patterns and configurations.

## Running the Example

```bash
cd examples/job_config
provisr serve job_config.toml
```

## Features Demonstrated

- Configuration-driven job definitions
- Multiple job types in one config
- Job templating and parameters
- Environment variable handling

Check the TOML configuration files for examples of different job patterns.