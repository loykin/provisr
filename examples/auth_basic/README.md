# Basic Authentication Example

This example demonstrates user creation, username/password login, JWT
authentication, and user listing with the public auth package.

```bash
cd examples/auth_basic
go run .
```

The example uses a local SQLite database. Replace the example JWT secret and
credentials before adapting it for a real environment.
